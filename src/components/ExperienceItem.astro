---
import Typography from '../layout/Typography.astro';
import { t, type Lang } from '../i18n';

interface Props {
    date: string;
    title: string;
    company: string;
    desc: string[];
    techStack?: string[];
    link?: string;
    lang?: Lang;
}

const { date, title, company, desc, techStack, link, lang = 'en' } = Astro.props;

---

<div class="relative mb-8">
    <div class="absolute w-4 h-4 bg-[#facc15] rounded-full top-[2.75rem] left-[-.5rem]"></div>
    <div class="ms-6 border-[1px] border-gray-200 dark:border-gray-700 rounded-lg p-4 bg-white/5 dark:bg-gray-800">
        <Typography as="time">{date}</Typography>
        <Typography as="h3" className="text-lg font-semibold text-[#facc15]" data-exp-title>{title}</Typography>
        <Typography as="h3" data-exp-company>{company}</Typography>
        <ul class="mt-2 space-y-1 text-gray-200">
            {desc.map((item, index) => (
                <li class="flex items-start">
                    <span class="mr-2">•</span> <span data-exp-desc>{item}</span>
                </li>
            ))}
        </ul>
        {techStack && (
            <div class="mt-2">
                <Typography as="span">
                    <strong data-tech-stack-label>{t(lang, 'tech_stack')}:</strong> {techStack.join(', ')}
                </Typography>
            </div>
        )}
    </div>
</div>


<!-- 
<div class="absolute w-3 h-3 bg-[#facc15] rounded-full mt-[0.45rem] -start-1.5"></div>
<time class="mb-1 text-sm font-normal leading-none text-white">{date}</time>
<h3 class="text-lg font-semibold text-[#facc15]">{title}</h3>
<p class="mb-4 text-base font-normal text-white">{desc}</p> -->